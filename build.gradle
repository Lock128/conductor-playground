buildscript {
  repositories {
    mavenLocal()
    // maven { url nexusAll }
    maven {
      url "https://plugins.gradle.org/m2/"
    }
  }
  dependencies {
  }
}

subprojects {
  task allDependencies(type: DependencyReportTask) {}
}